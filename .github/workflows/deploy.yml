name: Deploy

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  lint:
    runs-on: self-hosted
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v2
      - run: 
        pm2 stop gostev.site
        rm -rfv ~/gostev.site/
        mv ~/actions-runner/_work/gostev.site/gostev.site/ ~/.
